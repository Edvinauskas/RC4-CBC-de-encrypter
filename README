Takes a plain text file and encrypts it to RC4 (CBC mode). 
Program outputs ciphered text in hex. For decrypting text, the format of 
the HEX has to be the same as the encrypting output. Can encrypt from 1 - 256 bits. 
